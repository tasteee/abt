{
	"name": "solo",
	"version": "1.0.0",
	"private": true,
	"description": "A normal single-package repo. No workspace, no members.",
	"type": "module",
	"scripts": {
		"dev": "echo [solo] dev server on http://localhost:3000",
		"build": "echo [solo] building to dist/ with sourcemaps, minification and legacy browser targets",
		"test": "echo [solo] 12 passed, 0 failed",
		"typecheck": "echo [solo] no type errors",
		"lint": "echo [solo] no lint errors",
		"clean": "echo [solo] removed dist/",
		"fail": "echo [solo] this one exits 3 && exit 3"
	}
}
